# Core database driver (psycopg v3 — NOT psycopg2)
psycopg[binary]>=3.1
psycopg-pool>=3.1

# Data manipulation
pandas>=2.0
numpy>=1.24

# Geospatial
geopandas>=0.14
shapely>=2.0
pyproj>=3.6
fiona>=1.9

# Raster support (land-cover datasets)
rasterio>=1.3

# Environment configuration
python-dotenv>=1.0

# HTTP (optional FIRMS API)
requests>=2.31

# Structured logging
structlog>=23.0

# CLI
click>=8.1

# Data validation / models
pydantic>=2.0

# Dev / test dependencies
pytest>=7.4
pytest-cov>=4.1
